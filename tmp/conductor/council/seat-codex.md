# Seat: codex gpt-5.6-sol xhigh (read-only sandbox, session 01a04a7f-6bf4-71c1-96f1-e99a0143b166) — verbatim summary of the strict block
VERDICT: NO-GO · CONFIDENCE: 19
MUST-FIX: (1) credential-change recovery strands polling (snapshot replaced before a backoff-filtered empty poll). (2) total transient failure installs 2-min provider backoff AND a 1 s global timer → a worker thread every second polling nothing; a poll longer than its interval can keep POLL_GENERATION changing → continuous polling. Replace two clocks with one due-time scheduler. (3) pause/watch uses failures.first(); make pause/watch/backoff per provider. (4) MessageBoxW runs while the state mutex is held (tray.rs 527-536, 591-598) → modal loop dispatches timer/command → deadlock. (5) credential watching does sequential 5 s wsl.exe calls on the window thread. (6) provider-controlled timestamps can panic/hang (unchecked month index, huge-year loops, SystemTime add overflow). (7) refresh/probe runs real model requests (codex exec ., Claude Messages ".") — quota and an agent operating from $HOME; remove or use a non-inference refresh. (8) Grok token never refreshed; path-only watch. (9) Cursor copies state.vscdb into %TEMP%; abort leaves a DB with unrelated credentials. (10) portable updater accepts any unsigned .exe with no hash/Authenticode/size cap/rollback.
SHOULD-FIX: panel 2 Hz repaint + per-second cache parse; history rewritten whole with fsync; WSL timeout kills only wsl.exe (Linux child survives) — wrap in `timeout`, always kill/wait; Global mutex crosses users + FindWindow by title; no schema versions (parse failure silently resets); Store build still exposes --apply-update; CI does not assert tag == Cargo.toml version nor supply Store identity; TaskbarCreated NIM_ADD failures unlogged.
DEBT: centralize credential-source enumeration/winning-source cache/auth fallback/watch/refresh/retry; claude.rs duplicates wsl.rs; RequestFailed conflates rate-limit/network/parse; settings booleans + seats_for + --studio/widget debris; no tests for poll_worker, credential-change-before-backoff, mixed failures, WSL timeout cleanup, modal reentrancy, Taskbar restart, updater rollback; crash logging unbounded and takes the diagnose mutex first.
AUTH-TRANSPORT: no bearer tokens persisted (account ids are); native-first is not true fallback; static sh -lc reads workable, stdin refresh right, Claude's bash -lic defeats it; every poll can rescan every distro because the winning source is not cached; "Windows and WSL for every provider" needs fixtures per CLI/version + stale-source fallback + verified OpenCode/Fireworks/Devin paths.
RETRY-BUTTON: ProviderRuntime{backoff,paused,watch_snapshot,misses,last_manual_attempt}; PendingPoll{providers, force_once}; WM_APP_RETRY_PROVIDER WPARAM ProviderId+1; post_owner takes WPARAM; global + per-provider monotonic cooldown; retry clears only that provider; force bypasses backoff once; merge in-flight clicks; credential-file change uses the same targeted force path.
DISSENT-RISK: seeing watcher and backoff separately hides that updating the snapshot before an empty poll makes automatic recovery impossible.
