# Council — pre-ship code review of Headroom (2026-08-28)

Default mode, same prompt + same bundle (`headroom-review-bundle.md`, 7,965 lines) to every seat.
Seats: Gemini 3.7 Flash (agy, plan mode, raw fallback — the fleet MCP refuses `agy` on the
read-only lane), Fable 5 (anchor, high), Grok 4.6 (high; first attempt died at 21 s, retry ok),
Codex gpt-5.6-sol (xhigh). Verbatim seat outputs in `seat-*.md`; host synthesis in
`synthesis.md`; ledger entry appended to `~/projects/.claude/state/tb-council.json`.
