# Seat: claude-fable-5 (anchor, high) — verbatim summary of the strict block
VERDICT: GO-WITH-FIXES · CONFIDENCE: 14
MUST-FIX: (1) poll.rs handle_total_failure watches failures.first() only — fresh install (Claude+Grok) strands Grok. (2) grok.rs windows watch signature `windows|<path>` ignores size/mtime. (3) tray.rs on_poll_timer runs credential_watch_snapshot (wsl.exe, 5 s × distros) on the window thread — tray freezes each tick while paused. (4) run_with_timeout never drains stdout (4 KiB anon pipe; Codex auth.json larger) → false NoCredentials → 15-min backoff/pause [confidence likely].
SHOULD-FIX: timeout classified as NoCredentials; SetTimer/KillTimer from the worker thread; FindWindowW by title; Global\ mutex crosses users; fast-retry timer churn while everything is in backoff; docker-desktop probing (keeps the WSL VM alive, ~0.5–1 GB); 403 (Cloudflare) treated as auth; refresh spends quota (claude -p ., codex exec .) — throttle ≥10 min, prefer non-inference; poll_ok semantic hole; Cursor state.vscdb temp copy in %TEMP% under an old name.
DEBT: claude.rs re-implements wsl.rs (list_wsl_distros, run_with_timeout, decoders, wait_for_refresh); three credential-source shapes; Claude refresh via bash -lic; no tests for the poll state machine; vestigial artefacts (.__ccum_update_probe, claude-monitor-cursor-state-, widget/studio comments, --panel, seats_for); settings booleans; no schema_version; both processes load-modify-save settings.json; save_codex_credits writes every poll.
AUTH-TRANSPORT: split (argv reads / stdin refresh) sound; undermined by no drain, timeout≠missing, ~/.profile exec-zsh hazard, docker distros, default-user assumption; first source wins (second account invisible); Grok no refresh; watch/pause keyed to one provider. Token hygiene good.
RETRY-BUTTON: WM_APP_RETRY_PROVIDER (WPARAM ProviderId); forced ProviderSet + manual_retry_unix; 30 s per-provider mash guard; next_attempt_unix=0 keep misses; unpause + clear snapshot; retry_count=0; do_poll_once polled = due ∪ forced.swap(0); fetch-all = CMD_REFRESH + reset + SetTimer(interval) + 15 s global guard; LAST_REFRESH throttle ≥10 min for CLI turns; pending list in cache for "retrying…".
DISSENT-RISK: seats will burn budget on the POLL_GENERATION handshake (correct) and miss that the strands are semantic.
