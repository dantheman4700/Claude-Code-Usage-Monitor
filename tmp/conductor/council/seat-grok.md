# Seat: grok-4.6 high (grok CLI, --sandbox read-only) — verbatim summary of the strict block
VERDICT: GO-WITH-FIXES · CONFIDENCE: 16
MUST-FIX: (1) auth-pause + backoff deadlock: snapshot written first, request_poll finds nothing due, pause never lifts. (2) pause watches one provider, often Windows-only (ActiveSource): WSL files watched only under all_sources; sign-in elsewhere never lifts pause. (3) Grok Windows watch ignores in-place rewrites (modified() tested but not encoded). (4) existing Refresh already hammers on mash: clear_backoff + request_poll with no cooldown re-hits endpoints and re-runs codex exec/WSL refresh (90 s) — add per-provider min interval before adding buttons.
SHOULD-FIX: Windows-first credential wins even if dead (all but Claude) — fall back to next source on Auth/TokenExpired; Claude bypasses wsl.rs and refreshes via bash -lic; run_with_timeout no drain + kill leaves Linux child; portable updater ignores wait failure (Global mutex makes the relaunch exit); no download authenticity (any .exe); GetModuleFileNameW 260-char buffers; OpenCode WSL paths unverified, watch ignores all_sources; Devin/Fireworks watches incomplete; Claude Messages fallback spends quota; skip docker-desktop*; timers set from the worker.
DEBT: no shared credential-source/WSL-read/401 classification (eight copies); no settings_version (missing #[serde(default)] silently resets); vestigial seats_for, ~/.claude paths for Fireworks/Devin, temp-copy name, unused image/zip/D3D features; lock held across activity_log::record; tests miss pause×backoff×snapshot; diagnose log does not print tokens.
AUTH-TRANSPORT: native-first then quote-free sh -lc reads + stdin refresh is right; argv -lc for refresh is not (Claude); promise not kept: first Windows credential wins, ActiveSource omits WSL, OpenCode unverified, Grok never refreshes, docker distros probed. Need one CredentialSource iterator + shared 401→refresh-or-next-source + snapshots over the enabled set.
RETRY-BUTTON: WM_APP_RETRY_PROVIDER WPARAM ProviderId (0 = all); retry_providers(hwnd,set) never only clear_backoff; reset backoff for the set, unpause if it intersects the watch; gate 30 s after Auth/NoCreds and 2 s otherwise, show "wait Ns", do not queue; request_poll (coalescing is fine); never clear other providers' backoff; CMD_REFRESH = all with the same gate.
DISSENT-RISK: seats will call the strand a non-blocker because Refresh works, or flag the CAS as racy (it is fine); they will miss Grok's watch.
